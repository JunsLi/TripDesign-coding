%第三条链的起始都是C
function  flag = C_Climit(candidate,dim)
py = size(candidate,2);
count = 0;

    if candidate(1) ==2 ||  candidate(dim)==2
           count = count + 1;
    end

if count >0
    flag = 1;
else
    flag =0;
   
